2017-01-04
Starting this daybook. Here I try to order my thoughts about JS parametric editor
At this moment I decided to use next API for geom engine:
Lines and points are arrays.
Lines are normalized when created. No need to check in functions.

It will be CORE:
'core_point_int_line_line': function(rez, line0, line1) {
    bla bla
    return rez;
}
core_mid_point_point(rez, point, point);
core_point_per_point_line(rez, point, line);
core_point_line_circle_number(rez, line, circle, sign); // number of point if 2

core_line_lsd           (rez, line, side, distance);
core_line_point_point   (rez, point, point);
core_line_lineseg       (rez, lineseg); // lineseg.pnts
core_line_parallel_point_line   (rez, point, line)
core_line_per_point_line    (rez, point, line);
core_paramline_line     (rez, line)   // paramline[pnt, a, b]
    a*x+b*y+c=0  x=0, y=-c/b
    x=x0+ax*t
    y=y0+ay*t
    t=-x0/ax
    y = y0+ay*(-x0/ax)
    -c/b = y0 - ay*x0/ax

    a = -ay; b = ax; c = -(a*x0+b*y0);

core_line_circle_side_circle_side(rez, c0, s0, c1, s1); // empty array line if cant create
core_line_circle_line_side(rez, c0, l0, side); // line tangent to circle and parallel to line
    lcenter = core_line_parallel_point_line(lcenter, c0.pnts[0], l0);
    core_line_lsd(rez, lcenter, side, c0.r);

core_circle_tstsr(line,side, line,side, radius); // TTR with sides - intersection of 2 lsd lines
core_circle_tangent_circle_side_tangent_line_side_ipoint_radius(rez, c0, cside, l0, lside, ipoint, radius); // or empty array
core_circle_tangent_circle_side_tangent_circle_side_ipoint_radius

core_points_line_circle(rez, line, circle); // push only existing points
core_lines_circle_circle(rez, circle0, circle1);




'point=parallel(line,side,distance)&parallel('

'line=parallel(line,side,distance)' = function(rez, link) {
    line0 = link.main[0]; side0 = link.main[1];

'point=int(line,line)' = function(rez, link) {
    line0 = link.main[0];
}

'circle=TTR(line,side,line,side,radius)' = function(rez, link) {
    line0 = link.main[0]
}

'point=per(point, line)' = function(rez, link) {

}

'circle=tangent(line, side)&tangent(lineseg, side)&radius': function(rez, links) {
    line0 = links[0].main[0]; side0 = links[0].main[1];
    lineseg1 = links[1].main[0]; side1 = links[1].main[1];
     r = links[2].main[0];
     line0s = []; line0s = this['line=parallel(line,side,distance)'](line0s,
}

2017-01-05
changed document format. Each element type should be stored in its object (map ?)
 document format is 2 layered.
 1st layer is user-oriented. It stores symbolic links for objects etc.
 2nd layer is built from 1st and stored memory references for objects
 points and distances are stored in it's arrays and only 1 layer for them.

 add_object(type, ob) {
    if circle {
        pnts[pntid][0] = ob.x;
        pnts[pntid][1] = ob.y;
        dist[distid] = [ob.r]; // every dist element is an array
        el[elid] = {type: 'circle', data: ['pntid', distid]}
        pntid++; distid++; elid++;
    }
    if line el[elid] = {type: 'line', data: [ob[0], ob[1], ob[2]]};

 }

2017-01-11
Варианты названия диплома
Графический редактор с поддрежкой параметризации на базе HTML5
Разработка графического редактора с поддержкой геометрических взаимосвязей на базе HTML5

2017-01-12

The development of web based parametric 2D graphics editor
Разработка параметрического редактора 2D графики на базе веб-технологий.

Принципы работы геом ядра
every size of geom element should be potentially independent in case link will be removed
circleGetRadius(circle) { return circle[1][0]; };
every element contains data[ [points], [distances, angles, sides]]
this data CAN be substituted with other points or changed via links
link format:
{type: 'mid', linkedid: 'c1', linkedparam: 'circle_center', mainid: 'seg1'}
{type: 'mid', linkedid: 'c1.circle_center', mainid: 'seg1'}
param = docObjs.ob[first_part[pn[second_part]]]
c1.circle_center = mid(seg1)
{type: 'int', linkedid: 'p2', linkedparam: [], mainid: ['seg1', 'line2']}
p2 = int(seg1, line2)

{type: 'per_point_line', linkedid: 'line2', mainid: ['p1', 'line1']}
line2 = per_point_line(p1,line1)

line2 = horizontal()
line2 = coin(p3)  // will be converted to 'line_links_horizontal_coin_point'
{type: 'horizontal', linkedid: 'line2', mainid: []},
{type: 'coin', linkedid: 'line2', mainid: ['p3']}

line2 = tangent_circle_side_tangent_circle_side(c1, 1, c2, -1)
{type: 'tanget_circle ..', linkedid: 'line2', mainid: ['c1', 'c2'], data: [1, -1]}

line2 = tangent_circle_side('c1', 1)
line2 = coin_point('p1')

c5 = tstsr('line1', 1, line2, -1, 10)


param_names = {'circle_center': 0, 'circle_radius': 1}

2018-01-13
New idea - configuration of elements will be solved using hard links and 'soft' links - user input
when user creates element first time. Soft links will be used to make definition if hard links is not
enough.
Last entered soft links will have prioirity over first entered soft links.
seg(p1, p2)

p1.0 = 5 // soft (initial links)
p1.1 = 10
p1 = coin(line1) // hard link

p3 = mid(p1, p2)
p

2018-01-20
                                        Element language
p1 = pnt(20,30)
ls1= lineseg([20,20], p1)
ls2 = lineseg(ls1.p0, p1)
c1 = circle(p1, 10)
c1.c = mid([20,30], ls2.p0)
l1 = line([20, 30, 40]) // will be normalized to a^2 + b^2 == 1 at creation time
l1 = coin(ls1)
a1 = arc('c1', [ang1, ang2]) // will create full structure like at the next line
a1 = [[els['c1'][0]], els['c1'][1], [ang1, ang2]]
a2 = arc([pnts['p1']], [50], [0, pi/2]); // array of points, array of length, array of angles, array sides

2018-01-25
getpoints = function(ob) {
    type = ob.type; data = ob.data;
    switch(type) {
        case: 'lineseg': return(data);
        case: 'circle': return([data[0]]); // returns array with only 1 point
    }
}

move = function(ob, moveVec) {
    var t = getpoints(ob);
    for (i=0; i<t.length; i++) {
        t[i][0]+=moveVec[0];
        t[i][1]+=moveVec[1];
    }
}

scale = function(ob, base, scale) {
    var ps = getpoints(ob);
    var ls = getlength(ob);
    for (i=0; i<ps.length; i++) {
        d[0] = ps[i][0] - base[0]
        ps[i][0] = base[0] + d[0]*scale;
    }
    for (i=0; i<ls.length; i++) ls[i]*=scale;
}

equation of bisector line:
http://www.math-only-math.com/equations-of-the-bisectors-of-the-angles-between-two-straight-lines.html

2018-01-26
Creation of element:

p1 = pnt(20,30)

Its time to determine base terminology of editor:
what objects, what actions, at what coditions etc

it will be:
1. objects (points, elements, distances)
2. relationships (mid, per, coin, equal etc)

with all these will be next actions:
1. fillLinks (prepare links to calculate specific coordinates)
2. calcObjects (calc all dependent coordinates)
3. add new object or new link
4. remove object or link (in future)

2018-01-27
To create array of elements (dependent) its requires to create
each element separately and create link to the first element
c5.x = add(c1.x, 5)

New format of objects structure:
in each object will be stored reference to the function to solve this object.
Name of function will be calculated when scanning list of links,
because one function can solve several links (through point and parallel line)
OLD:
docObjs[id] = {type: els[id].type, ob: els[id], id: id, solved: true, mainids: [], linkids: [], links: [], query: els[id].type+'_links'};
NEW:
docObjs[id] = {type: els[id].type, ob: els[id], id: id, solved: true, mainids: [], linkids: [], ****mains[], links: [], query: els[id].type+'_links', *****func: ''};
pnts, lens, angles, dirs, sides
while solving cycle:
if readyto solve && not solved
    ob.func(ob.els[id], ob.mains)
    ob.solved = true;

also scale, movement and rotation can be done with affine matrixes in homogenius coordinates
for instance array 1..3 will look
c1 = [[20,30], [50]]
c2 = transform(c1, 'move', [20, 0])
c3 = copy(c1, [40, 0]);

copyOb = function(ob) { var rez = [];
    for (i=0; i<; i++) {
        rez[i] = [];
        for (j=0; j<ob[i].length; i++) {
            rez[i][j] = ob[i][j];
        }
        }
    }
}

copy = function(ob, vec) {
    pnts = get_points(ob)
    for (i=0; i<pnts.length; i++) pnts[i][0]+=ob[0] ...
}

2018-01-28
If links is not enough to solve element, information will be taken from
PNTS array of element data structure
This will be detected when there are no functions to solve element
Then to solution query will be added 'coin_point' and search again
IMPORTANT:
This requires all solution functions will fill result after
all solution have done.
PS: 'coin_point' to circle and arc means that point coin with center

To not make me crazy, access to the element's structure will be using API,
-------------------------------------------------------------------------------------------
getPoints, getDists, getAngles, getDirs (arrays of directional cosines), getSides, getTexts, getData
All these functions must return arrays of appropirate data, when given object's data.

NEW line format:
[[a,b], c]
CORRECTION:
it is no need to make [[a,b],c]
because it can be processed [a, b, c] without problem

internally all angles will be in sin/cos

2018-01-29
Main idea is to create special functions for specific cases and if no special function,
then use common function, for instance, common scale is to move points from base.
This general idea could also be used even for links, for instance, link 'coin'
can be implemented by putting perpendicular from all points to main line.
Also 'stupid perpendicular' can be implemented by putting point1 to main line,
using point0 as base. Thats all. Ignore point2, point3 etc.

Also, copy operation COULD also copy links (optionally). It can be useful when creating arrays.

2018-02-03
c1 = scale('c1', [50, 30], 1.5)
c1 = scale('c1', 'p5', 1.5)
c2 = scale(c1, p5, 1.5)/link

general architercture
UI > text > parser > structure
editing will be to text, then reparse
structure:
each type will be in separate array,
all elements/points/distances will be in Big Objects Array
links will be in links array OR maybe parser will parse link/linkset to functions
and set functions to objects

when creating complex elements, like lineseg, it should to create simple elements
like 'lineseg1.p0' and set link to them from 'lineseg1'
reason:
1. can be programmatically (and even from text) accessed parts of complex elements,
for set links (or references) to/from these parts
2. unified API to every element (simple 1 dimentional object)
cons:
still no easy way to set links to non-referenced parameters, like circle radius
every time when distance 'c1.r' changes it should be changed at 'c1' object via link solver

c1.r = distance(p1,p5)  // will create two links - 'distance(p1,p5)' will be linked to distance between points
and 'c1.r' will be linked to 'distance(p1,p5)'
c1.r = c2.r // simple link equal (copy value from one to another)
ls1.p0.x = ls2.p1.y // link between two distances and then link to ls1.p0  !!!!!!!!!!!!!!!!!!!!!!
p3 = mid(p1,p2) // will create new object
ls1.p1=mid(p1,p2) // modify existing (add link) to point ls1.p1

should be object values accessor -
acor.get(obdata, type, prop) {
    var func_name = type + '_get_' + prop;
    return acor[func_name](obdata);
}
acor.set(obdata, type, propName, propValue) {
    var func_name = type + '_set_' + propName;
    return acor[func_name](obdata, propValue);
}

use JSON.parse:
p1 = pnt([2,3])
p1.x = p2.x+5

2018-02-09
Should be more functionality in parser:
1. subsitution p1[0] instead of p1.x
2. add doc.objs['p1'] instaed of simply 'p1'
OR it should add a function
'p1.x+p2y' = [add, 'p1.x', 'p2.x']
add = function(v1, v2) { return doc.objs[v1].ob+doc.objs[v2] }
'mid(p1,p2)' = [mid, 'p1', 'p2']
mid = function(v1, v2) {
 var x, y;
 x= (doc.objs[v1].ob[0] + doc.objs[v2].ob[0])/2;
 y= (doc.objs[v1].ob[1] + doc.objs[v2].ob[1])/2;
 return [x,y];
}

p1 = int(l1, l2)
// function set explisitly and pars are passed by IDs
doc.objs['p1'].link = [int_line_line, ['p1', 'l1', 'l2']]    // its easy to do links[0] later, but I think its enough just 1
when called it will be:
var pars = [];
var parids = doc.objs['p1'].link[1];
for (i=0; i<parids.length; i++) {
    pars.push(doc.objs[parids[i]].ob)
}
doc.objs['p1'].link[0].apply(this, pars);  // will be passed like  --- mid(p1, p2) // no ids, just data

2018-02-10
For scalar data types (length, side) will be reference format:
[array, index]
for points, and more complex objects will be usual reference for array/object
this reference will be used this way:
add = funciton(rez, a, b) { // maybe params will be in array ...
    rez[0][rez[1]]=a[0][a[1]] + b[0][b[1]];
    return rez; // will return same reference with new data
}
in the objs array for scalar data it will be stored in typed arrays (pnts, dists, circles)

ls1.0.1 // ls1.p0.y
OF CAUSE, if using .0, number should be converted to int with parseInt
to get reference for y simply take nearest parent and index:
var t = ls1['p0']
t['y']  OR
var t = ls1[0];
t[1]  // short name to access

// example of using
var trez = ls2[1]; // second point of line segment
var t1 = p5;
var dist1 = objs.ob.dist1
add([trez,1], [t1, 1], [dist1, 0]);
when parsing and placing links it will be caclulated 'short references'
[ref, index]
and placed to links data for function processing
I hope I can use same syntax for integer and string indexes // just checked - should work

to not forget: points we getting using geom_core.get_points

2018-02-11
Its time to get something working:
points, lines, linesegs, dists

ways to manage refs:
if point, ref to point
if line, ref to line
if point coord, ref to [point, number]
if dist - dist is array of one element // maybe should be 'scalar' type - for dists, angles, sides
so, its will be scalarref - access via scalarref[0]
type of ref will be known when placing link functions. Each link function is specific to specific types
of arguments and results.
RESUME: [ref, number] will be used only for points at this time. Later maybe array of scalars

NEW:
ls1.p0.x
c1.center.x
c1.r / c1[1]
arc1.c1.center.x
arc1.c1.r
IDEA:
till the last dot, objects will store in appropirate arrays, for instance
it will be point
pnts['c1.center'] = [20, 30];
or circles['arc1.circle'] = [pnts['c1.center'], 50];
arcs['arc1'] = [circles['arc1.center'], [p2/4, pi/2]];
Because ID at typed arrays and object array are same, we can easily get type of every object.
So we getting type of ID and if its not scalar, we using ref to object.
If type is absent (for instance coordinate of point dosnt have individual record), we using parent
object (get it by trimmed ID) and using [pnt, coord_number] for later using obj[index]. Therefore
we dealing with scalar data in more complex objects

if type is standalone scalar (dist etc), store it in 1 item array and using [dist_array, 0]

2018-02-14
Lets try to implement lineseg and mid
When create element, first, create simply using given points, even if links are specified
At the next string of CLI specify link:
ls2=lineseg(ls2.0, ls2.1)
ls2.1=mid(ls1.0,ls1.1)

At first time all CLI commands will be very detailed

2018-02-19
It should be functions .r, .c (.center) in element's file which return link

move(el, p0, p1)


2018-02-20
format of

done first draw (without snap)

2018-02-21
continuing to clean and create the app.
Try to implement 'mid'

2018-02-24
New architecture is slowly have been fulfilled.
Implemented mid, eq, int types of links. Next step is light refactoring of the parser.

Equation of line, perpendicular to line and through point

2018-02-26
Continue to work with new features. Stopped at the circle_TTRS link.
Here some thoughts about this problem.
parser should create complete data set for solver.
This data set should be adressable by some name.
Here are variants:
1. Store link data in .main property of the linked element. Can be addressed by el.main.field1
2. Store link as individual object and set link to this link :)  linkName.field1

2018-02-28
How to enter data. Example lineseg:

2018-03-01
Circle by 3 lines
a0x + b0y + r = -c
a1x + b1y + r = -c
a2x + b2y + r = -c

write determinant for 3x3 matrix to geom_core and use it for this task;

2018-03-03
There are many rulers (more then steps for building element). So it will be WAYS for
creating element. WAY is array of numbers of rules which participate in element creation.
update:
Ways are implemented.

2018-03-04
links should be in separate array.
To be able to work without parent id
because when select point for 'mid' we must also find element id

2018-03-05
simple geom equation:
0*x + 1 * y - 100 = 0


a*x + b* y = R
a*a + b*b = 1
a = (R-b*y)/x
(R-b*y)^2 + b^2*x^2 = x^2

IDEA:
elements will exchage set of parameters

2018-03-06

sample of new links table:

tan(c5,l2, 1) // 1 - side selector
per(l2,l3)
